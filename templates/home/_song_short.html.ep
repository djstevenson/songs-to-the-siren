<a href="<%= url_for('view_song', song_id => $song->id) %>">
<span class="title"><%= $song->title %></span>
</a>
-
<span class="artist"><%= $song->artist %></span> : <span class="date"><%= datetime($song->published_at) %></span>
