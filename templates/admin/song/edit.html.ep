% layout 'default';
% title 'Edit song: ' . $song->title;

<%== $form->render %>

<p>
    <a target="_blank" href="<%= url_for('admin_list_song_links', song_id => $song->id) %>">Edit links for <%= $song->title %></a>
</p>
<p>
    <a target="_blank" href="<%= url_for('admin_edit_song_tags', song_id => $song->id) %>">Edit tags for <%= $song->title %></a>
</p>
