<p class="login-status">
% if (my $auth_user = stash 'auth_user') {
    Logged-in as <span class="user-name"><%= $auth_user->name %></span>
    % if ($auth_user->admin) {
        (ADMIN)
    % }
    <a href="<%= url_for('logout') %>">Logout</a>
% }
% else {
    <a href="<%= url_for('login') %>">Login</a> to add comments
% }
</p>
