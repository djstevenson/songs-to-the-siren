% layout 'admin';
% title 'Edit tags for ' . $song->title;

<h2>Edit tags for <%= $song->title %></h2>

<%== $form->render %>

<ul class="tag-list">
    % foreach my $tag ( @$tags ) {
        <li>
            %= include 'song/tag/_deletable_tag', tag => $tag
        </li>
    % }
</ul>
