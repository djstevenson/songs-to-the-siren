% if ( exists $links->{YouTube} ) {
    % my $ytlink = $links->{YouTube};
    <div class="embed-responsive embed-responsive-16by9">
        <iframe
            class="embed-responsive-item"
            src="<%= $ytlink->url %>?rel=0"
            allowfullscreen>
        </iframe>
    </div>
% }
