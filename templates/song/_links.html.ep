% if ( scalar keys %$links ) {
    <div class="links">
        <h4>Links</h4>
        <p>
            <ul class="link-list">
                % while ( my $link = $list_links->next ) {
                    <li class="<%= $link->css %>">
                        <a href="<%= $link->url %>"><%= $link->description %></a>
                    </li>
                % }
            </ul>
        </p>
    </div>
% }
