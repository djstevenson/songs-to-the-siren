{
    # The app will, if they exist, override these values with the env vars:
    #  SONGSTOTHESIREN_SMTP_HOST=...
    #  SONGSTOTHESIREN_SMTP_USER=...
    #  SONGSTOTHESIREN_SMTP_PASSWORD=...
    #  SONGSTOTHESIREN_SMTP_FROM=...
    #
    # You can set them here, but then you'll want to be careful not to 
    # make this file public e.g. on github. I recommend using ENV vars.
    smtp => {
        host     => 'your.smtp.host.example.com',
        user     => 'smtp.user.name',
        password => 'smtp.user.password',
        from     => 'noreply@songstothesiren.com',
    },

    link_roles => [qw/
        Default
        YouTubeEmbedded
    /],
}
